<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
  </head>
  <body>
    <div>111</div>
    <script src="/js/jquery.js"></script>
    <script>
      const httpUrl = './search?searchKey=故宫'
      console.log(httpUrl);
      $.get(httpUrl).then(res => {
        console.log(res)
        res.forEach((item, i) => {
          $('body').append('<h1>' + item.bookname + '</h1>')
        })
      })

      // const httpUrl = './searchPost?searchKey=故宫'
      // $.post(httpUrl).then(res => {
      //   console.log(res)
      //   res.forEach((item, i) => {
      //     $('body').append('<h1>' + item.bookname + '</h1>')
      //   })
      // })
    </script>
  </body>
</html>
